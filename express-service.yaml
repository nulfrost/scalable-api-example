apiVersion: v1
kind: Service
metadata:
  name: express-service
spec:
  selector:
    app: express-server
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      nodePort: 30000
